---
import BaseLayout from '../layouts/BaseLayout.astro';
import FlowMotif from '../components/FlowMotif.astro';
import { getCopy } from '../i18n/copy';
import { pathFor, type Locale, withBase } from '../i18n/utils';
import { getProfilePath } from '../i18n/app-profiles';

interface Props {
  locale: Locale;
  path: string;
}

const { locale, path } = Astro.props;
const t = getCopy(locale);
const quickTags =
  locale === 'zh'
    ? ['#在场', '#轻结构', '#数字健康', '#ADHD友好', '#反成瘾']
    : ['#presence', '#gentle-structure', '#digital-wellbeing', '#ADHD-friendly', '#anti-addictive'];
const appMotifs: Array<'cat' | 'wave' | 'rings' | 'breathe'> = ['cat', 'breathe', 'wave', 'rings'];
const contrastPanels =
  locale === 'zh'
    ? [
        {
          label: 'Before',
          title: '被提示、热搜和自动化拉走',
          lines: ['刚起念头，就被别的刺激截断。', '习惯还没开始，评价已经先到了。']
        },
        {
          label: 'After',
          title: '给注意力一点空气，再决定下一步',
          lines: ['先接住，再整理。', '先回到当下，再进入节奏。']
        }
      ]
    : [
        {
          label: 'Before',
          title: 'Pulled by prompts, trends, and autoplay loops',
          lines: ['A thought appears and is interrupted instantly.', 'Judgment arrives before any rhythm can form.']
        },
        {
          label: 'After',
          title: 'Give attention some air before deciding the next move',
          lines: ['Catch first, then organize.', 'Return to the moment before entering the day.']
        }
      ];
const labels =
  locale === 'zh'
    ? {
        story: '品牌故事',
        quote: '立场',
        principles: '原则',
        products: '产品方向',
        focus: '参考方向'
      }
    : {
        story: 'Brand Story',
        quote: 'Position',
        principles: 'Principles',
        products: 'Products',
        focus: 'Focus'
      };
---

<BaseLayout title={t.meta.siteName} description={t.meta.description} locale={locale} path={path}>
  <section class="hero hero-v2 shell editorial-hero">
    <div class="hero-copy hero-copy-editorial">
      <p class="eyebrow">{t.hero.eyebrow}</p>
      <h1>{t.hero.title}</h1>
      <p class="lead">{t.hero.tagline}</p>
      <div class="hero-cta">
        <a class="btn btn-primary" href={withBase(pathFor(locale, 'apps'))}>{t.hero.primaryCta}</a>
        <a class="btn btn-secondary" href={`#${t.story.id}`}>{t.hero.secondaryCta}</a>
      </div>
      <p class="calm-note">{t.hero.calmNote}</p>
      <div class="hero-footnote">
        <span>{labels.focus}</span>
        <div class="tag-row">
          {quickTags.slice(0, 4).map((tag) => (
            <span class="tag-pill">{tag}</span>
          ))}
        </div>
      </div>
    </div>
    <div class="contrast-stack" aria-label={locale === 'zh' ? '对照' : 'Contrast'}>
      {contrastPanels.map((panel, index) => (
        <article class={`contrast-panel ${index === 1 ? 'contrast-panel-strong' : ''}`}>
          <p class="tile-kicker">{panel.label}</p>
          <h3>{panel.title}</h3>
          <div class="contrast-lines">
            {panel.lines.map((line) => <p>{line}</p>)}
          </div>
        </article>
      ))}
      <div class="hero-motif-wrap" aria-hidden="true">
        <FlowMotif kind="wave" />
        <FlowMotif kind="rings" />
      </div>
    </div>
  </section>

  <section class="shell story" id={t.story.id}>
    <div class="story-head">
      <div class="section-intro story-intro">
        <p class="eyebrow">{labels.story}</p>
        <h2>{t.story.title}</h2>
        <p>{t.story.intro}</p>
      </div>
      <blockquote class="quote story-quote">
        <p class="story-quote-label">{labels.quote}</p>
        <p>{t.story.quote}</p>
      </blockquote>
    </div>
    <div class="story-grid">
      {t.story.blocks.slice(0, 3).map((block, index) => (
        <article class={`story-block ${index === 0 ? 'story-block-featured' : ''}`}>
          <p class="story-index">{String(index + 1).padStart(2, '0')}</p>
          <h3>{block.title}</h3>
          <p>{block.body[0]}</p>
        </article>
      ))}
    </div>
  </section>

  <section class="shell">
    <div class="section-intro">
      <p class="eyebrow">{labels.principles}</p>
      <h2>{t.principles.title}</h2>
      <p>{t.principles.intro}</p>
    </div>
    <div class="principle-list">
      {t.principles.items.map((item, index) => (
        <article class="principle-row">
          <p class="story-index">{String(index + 1).padStart(2, '0')}</p>
          <div class="principle-row-main">
            <h3>{item.title}</h3>
            <p>{item.body}</p>
          </div>
          <p class="principle-icon" aria-hidden="true">{item.icon}</p>
        </article>
      ))}
    </div>
  </section>

  <section class="shell" aria-labelledby="apps-showcase-title">
    <div class="section-intro section-split">
      <div>
        <p class="eyebrow">{labels.products}</p>
        <h2 id="apps-showcase-title">{t.apps.title}</h2>
      </div>
      <p>{t.apps.intro}</p>
    </div>
    <div class="product-list">
      {t.apps.items.map((app, index) => (
        <article class={`product-row ${index === 0 ? 'product-row-featured' : ''}`} id={`home-${app.id}`}>
          <div class="product-row-index">
            <span>{String(index + 1).padStart(2, '0')}</span>
          </div>
          <div class="product-row-copy">
            <p class="tile-kicker">{app.nameEn}</p>
            <h3>{app.name}</h3>
            <p class="lead-sm">{app.oneLine}</p>
            <ul class="product-bullets">
              {app.bullets.slice(0, index === 0 ? 3 : 2).map((item) => <li>{item}</li>)}
            </ul>
            <a class="text-link" href={withBase(getProfilePath(locale, app.id) ?? pathFor(locale, 'apps'))}>
              {t.apps.learnMore}
            </a>
          </div>
          <div class="product-row-art" aria-hidden="true">
            <FlowMotif kind={appMotifs[index] ?? 'wave'} />
          </div>
        </article>
      ))}
    </div>
  </section>
</BaseLayout>
